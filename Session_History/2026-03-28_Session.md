# Session — 2026-03-28

## Summary
Completed `/polish` pass on all 6 pages of dojix-site. Fixed accessibility, copy consistency, SEO, and visual layout issues.

## What Was Built/Changed

### Polish Pass (all pages)
- **Refund discrepancy fix**: FAQ said "30 days", Terms said "14 days" — standardized FAQ + JSON-LD to 14 days (Terms is authoritative)
- **Footer link text**: "FAQ" → "Support" across all 5 page footers (matches nav terminology)
- **Hamburger accessibility**: Added `aria-expanded` attribute toggling on all hamburger buttons
- **Trailing slash consistency**: All internal links to `/privacy` and `/terms` updated to use trailing slashes (`/privacy/`, `/terms/`)
- **Canonical/OG URL fix**: Privacy and terms pages' canonical and og:url updated with trailing slashes
- **Sitemap fix**: Privacy and terms URLs updated with trailing slashes

### Privacy & Terms Pages
- Added `--accent-hover:#f0c060` CSS token (was hardcoded hex)
- Replaced hardcoded `#f0c060` in `.btn-waitlist:hover` with `var(--accent-hover)`
- Wrapped footer in `<div class="container">` for consistent layout with other pages
- Added `@media (prefers-reduced-motion:reduce)` media query

### Privacy Page Only
- Added `<caption class="sr-only">` to third-party services table

### Homepage
- Replaced `alert()` in waitlist form error handler with inline error message (`#waitlist-error`)
- Fixed feature grid card 06 ("100% Local. Zero Cloud.") — now spans full grid width instead of orphaned alone on last row

### 404 Page
- Added `<meta name="robots" content="noindex">`
- Added `@media (prefers-reduced-motion:reduce)` media query

## Decisions Made
- Refund period: 14 days (matching Terms of Service, the binding legal document)
- Footer terminology: "Support" not "FAQ" (consistent with nav)
- Card 06 spans full width for balanced visual layout
- All directory-based pages use trailing slash URLs

## Files Modified
- `index.html` — waitlist error handling, footer link text, hamburger aria-expanded, card 06 grid span, trailing slashes
- `features/index.html` — footer link text, hamburger aria-expanded, trailing slashes
- `support/index.html` — refund 30→14 days (FAQ + JSON-LD), footer link text, hamburger aria-expanded, trailing slashes
- `privacy/index.html` — accent-hover token, footer container wrap, reduced motion, table caption, hamburger aria-expanded, trailing slashes, canonical/OG URLs
- `terms/index.html` — accent-hover token, footer container wrap, reduced motion, hamburger aria-expanded, trailing slashes, canonical/OG URLs
- `404.html` — noindex meta, reduced motion
- `sitemap.xml` — trailing slashes on privacy/terms URLs
- `CLAUDE.md` — updated current state, critique history, conventions

## Current Status
- All 6 pages polished and consistent
- Rollback tag: `pre-polish-v1` available for revert
- Formspree waitlist live and working (f/xykbkzdb → doug@dojix.app)
- Paddle business verification: still in review (only launch blocker)

### Cross-Repo: Dojix Dashboard (EdgeLens repo)
- Created `dashboard.html` with 8 collapsible sections and embedded `DASHBOARD_DATA` object
- Created `/dashboard-update` skill at `~/.claude/skills/dashboard-update/SKILL.md`
- Created `Dojix Dashboard.app` bundle at `~/Applications/Dashboards/`
- Updated dashboard skill and edgelens skill with Dojix dashboard references

## Next Steps
1. Paddle business verification approval (waiting)
2. File USPTO trademark for "Dojix" (~$250)
3. Register copyright at copyright.gov (~$65)
4. When Paddle approves: swap waitlist CTAs to checkout, launch Reddit ads
